# Platzhalter-Bilder

Folgende Dateien sind **generierte Platzhalter** bzw. fehlen noch komplett
und müssen vor dem Live-Gang ersetzt werden:

- `logo.png` – aktuell ein generierter Platzhalter (Streifenmuster), keine
  echte Bilddatei. Durch das echte FehlerFix-Logo ersetzen (Original-
  Auflösung, transparenter Hintergrund empfohlen). Einfach mit gleichem
  Dateinamen überschreiben.
- `team-foto.png` – Platzhalter. Durch das echte Team-Foto (Mariam, Salvador,
  Blanca) ersetzen, Seitenverhältnis ca. 3:4 passt am besten zu
  `components/TeamSection.tsx`.
- `business-school-1.jpg` bis `business-school-6.jpg` – **existieren noch
  gar nicht** und werden von `components/BusinessSchoolCarousel.tsx`
  referenziert. Echte Fotos vom Business@School-Wettbewerb dort ablegen
  (gleiche Dateinamen verwenden, dann muss im Code nichts angepasst werden).
  Bis dahin zeigt der Browser an diesen Stellen ein gebrochenes Bild-Icon.
